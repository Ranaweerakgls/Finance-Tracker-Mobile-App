<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Financetracker" parent="Theme.MaterialComponents">
        <item name="colorPrimary">@color/primary_dark</item> <!-- dark variant -->
        <item name="colorPrimaryDark">@color/primary</item> <!-- invert if needed -->
        <item name="colorAccent">@color/accent</item>
        <item name="android:textColorPrimary">@color/text_primary_dark</item>
        <item name="android:textColorSecondary">@color/text_secondary_dark</item>
        <item name="android:windowBackground">@color/background_dark</item>

        <!-- Remove ActionBar in dark mode too -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>
